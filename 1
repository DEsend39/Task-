#include <iostream>
#include <vector>
#include <algorithm> 

int main() {
    std::vector<int> arr = {4, 5, 6, 4, 7, 8, 5, 9, 10, 6, 11};

    std::sort(arr.begin(), arr.end());

    auto last = std::unique(arr.begin(), arr.end());

    arr.erase(last, arr.end());

    std::cout << "Уникальные значения: ";
    for (const int& num : arr) {
        std::cout << num << " ";
    }
    std::cout << std::endl;

}
